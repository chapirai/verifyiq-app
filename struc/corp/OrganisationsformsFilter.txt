Schema name: OrganisationsformsFilter
API: Bolagsverket Person API
Response path: OrganisationsengagemangRequest.filtrering.*

Raw structure:
{
"attribut": "string",
"varde": "AB"
}

Fields:

* attribut: string
* varde: string

Mapping:

* attribut -> attribut
* varde -> varde

Rules:

* type conversion: ensure attribut and varde are strings
* optional/missing handling: both attribut and varde are required; object should be rejected if either is missing
* special parsing: validate varde against allowed enum values (AB, BAB, BF, BFL, BRF, E, EB, EEIG, EGTS, EK, FAB, FF, FL, FOF, HB, I, KB, KHF, MB, OFB, OTPB, S, SB, SCE, SE, SF, TPAB, TPF, TSF)

Target type:
{
"attribut": "string",
"varde": "string"
}
