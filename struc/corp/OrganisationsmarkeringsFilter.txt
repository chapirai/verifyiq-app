Schema name: OrganisationsmarkeringsFilter
API: Bolagsverket Person API
Response path: OrganisationsengagemangRequest.filtrering.*

Raw structure:
{
"attribut": "string",
"varde": "LAGER"
}

Fields:

* attribut: string
* varde: string

Mapping:

* attribut -> attribut
* varde -> varde

Rules:

* type conversion: ensure attribut and varde are strings
* optional/missing handling: both attribut and varde are required; object should be rejected if either is missing
* special parsing: validate varde against allowed enum value (LAGER)

Target type:
{
"attribut": "string",
"varde": "string"
}
